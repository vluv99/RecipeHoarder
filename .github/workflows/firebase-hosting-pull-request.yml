# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@master
        with:
         node-version: '14.x'

      - name: Download npm
        run: npm install

      - name: Download angluar
        run: npm install @angular/cli

      - name: Build the angular
        run: npm run build:prod

      - run: |
          cd dist
          tree -a
          ls
          ls ./recipe-hoarder

#      - uses: FirebaseExtended/action-hosting-deploy@main
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPE_HOARDER }}'
#          projectId: recipe-hoarder
#          expires: 30d

      - uses: w9jds/firebase-action@master
        with:
          args: hosting:channel:deploy pr-${{ github.event.number }}
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

